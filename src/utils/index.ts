/**
 * Export all utils defined in this folder from here
 */

import { iconSelector } from './iconSelector';
import { isValidMail, lengthCheck, dataValidation } from './inputValidator';
import { isObjectEmpty } from './objectEmptyCheck';

export {
  iconSelector,
  isValidMail,
  lengthCheck,
  dataValidation,
  isObjectEmpty,
};
